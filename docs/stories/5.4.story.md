# Story 5.4: Route Structure and Role-Based Dashboard Stubs

## Status

**Done** (Session 59)

## Story

**As a** authenticated business user (Producer, Distributor, or Retailer),
**I want** to be redirected to my role-specific dashboard after login,
**so that** I see relevant content and actions for my role immediately.

## Acceptance Criteria

1. `/dashboard` page converted to role-based router (redirects to role-specific pages)
2. PRODUCER users redirected to `/producer/dashboard`
3. DISTRIBUTOR users redirected to `/distributor/dashboard`
4. RETAILER users redirected to `/retailer/dashboard`
5. PLATFORM_ADMIN users redirected to `/admin` (existing behavior)
6. `/producer/dashboard` stub page created with "Register Product" button
7. `/distributor/dashboard` stub page created with placeholder content
8. `/retailer/dashboard` stub page created with placeholder content
9. All stub pages use Layout component and theme tokens
10. All stub pages have role-based access protection (getServerSideProps)
11. Unauthenticated users redirected to `/login`

## Tasks / Subtasks

- [x] **Task 1: Convert /dashboard.tsx to Role-Based Router** (AC: 1-5, 11)
  - [x] Update `src/pages/dashboard.tsx`
  - [x] Remove stub content (welcome message)
  - [x] Add role-based redirect logic in getServerSideProps
  - [x] Redirect PRODUCER → `/producer/dashboard`
  - [x] Redirect DISTRIBUTOR → `/distributor/dashboard`
  - [x] Redirect RETAILER → `/retailer/dashboard`
  - [x] Keep existing PLATFORM_ADMIN → `/admin` redirect
  - [x] Redirect unauthenticated → `/login`
  - [x] Keep component for COMPANY_ADMIN (no dedicated dashboard yet)

- [x] **Task 2: Create Producer Dashboard Stub** (AC: 6, 9, 10)
  - [x] Create `src/pages/producer/` folder
  - [x] Create `src/pages/producer/dashboard.tsx`
  - [x] Add getServerSideProps with PRODUCER role check
  - [x] Redirect to /dashboard if not PRODUCER role
  - [x] Use Layout component
  - [x] Display "Producer Dashboard" heading
  - [x] Add "Register Product" button linking to `/producer/register`
  - [x] Add placeholder text: "Product list coming in Epic 12"

- [x] **Task 3: Create Distributor Dashboard Stub** (AC: 7, 9, 10)
  - [x] Create `src/pages/distributor/` folder
  - [x] Create `src/pages/distributor/dashboard.tsx`
  - [x] Add getServerSideProps with DISTRIBUTOR role check
  - [x] Redirect to /dashboard if not DISTRIBUTOR role
  - [x] Use Layout component
  - [x] Display "Distributor Dashboard" heading
  - [x] Add placeholder text: "Receive products feature coming in Epic 6"

- [x] **Task 4: Create Retailer Dashboard Stub** (AC: 8, 9, 10)
  - [x] Create `src/pages/retailer/` folder
  - [x] Create `src/pages/retailer/dashboard.tsx`
  - [x] Add getServerSideProps with RETAILER role check
  - [x] Redirect to /dashboard if not RETAILER role
  - [x] Use Layout component
  - [x] Display "Retailer Dashboard" heading
  - [x] Add placeholder text: "Product management coming in Epic 7"

- [x] **Task 5: Manual Testing** (AC: 1-11) - Verified by user (Session 59)
  - [x] Test login as PRODUCER → lands on `/producer/dashboard`
  - [x] Test login as DISTRIBUTOR → lands on `/distributor/dashboard`
  - [x] Test login as RETAILER → lands on `/retailer/dashboard`
  - [x] Test login as PLATFORM_ADMIN → lands on `/admin`
  - [x] Test direct access to `/dashboard` → redirects based on role
  - [x] Test direct access to `/producer/dashboard` as non-PRODUCER → blocked
  - [x] Test unauthenticated access → redirected to `/login`

## Dev Notes

### Story Type

**DEV STORY** - Frontend routing and page structure. No QA required (stub pages, easy to fix later).

### Epic Reference

**Epic 5: Product Registration - Frontend Preparation**
- This story establishes the route structure for Story 5.5 (registration form)
- Assigned to: Sam/YiLing
- No QA required (routing/stubs)

### Technical Specifications

**Role-Based Router Pattern:**

```typescript
// src/pages/dashboard.tsx - Becomes a router, not a page
import { GetServerSidePropsContext } from 'next';
import { getServerSession } from 'next-auth';
import { authOptions } from '@/pages/api/auth/[...nextauth]';

export async function getServerSideProps(context: GetServerSidePropsContext) {
  const session = await getServerSession(context.req, context.res, authOptions);

  // Not authenticated → login
  if (!session) {
    return {
      redirect: { destination: '/login', permanent: false },
    };
  }

  // Role-based redirects
  const redirectMap: Record<string, string> = {
    'PRODUCER': '/producer/dashboard',
    'DISTRIBUTOR': '/distributor/dashboard',
    'RETAILER': '/retailer/dashboard',
    'PLATFORM_ADMIN': '/admin',
  };

  const destination = redirectMap[session.user.role] || '/login';

  return {
    redirect: { destination, permanent: false },
  };
}

// This component never renders (always redirects)
export default function DashboardRouter() {
  return null;
}
```

**Role-Protected Page Pattern:**

```typescript
// src/pages/producer/dashboard.tsx
import { GetServerSidePropsContext } from 'next';
import { getServerSession } from 'next-auth';
import { authOptions } from '@/pages/api/auth/[...nextauth]';
import { Box, Heading, Text, Button, VStack } from '@chakra-ui/react';
import { Layout } from '@/components/layout';
import NextLink from 'next/link';

export async function getServerSideProps(context: GetServerSidePropsContext) {
  const session = await getServerSession(context.req, context.res, authOptions);

  if (!session) {
    return { redirect: { destination: '/login', permanent: false } };
  }

  if (session.user.role !== 'PRODUCER') {
    return { redirect: { destination: '/dashboard', permanent: false } };
  }

  return {
    props: {
      userName: session.user.name || session.user.email,
    },
  };
}

interface ProducerDashboardProps {
  userName: string;
}

export default function ProducerDashboard({ userName }: ProducerDashboardProps) {
  return (
    <Layout>
      <VStack spacing={6} align="stretch">
        <Heading color="brand.primary">Producer Dashboard</Heading>
        <Text color="brand.muted">Welcome, {userName}</Text>

        <Box>
          <Button as={NextLink} href="/producer/register" size="lg">
            Register New Product
          </Button>
        </Box>

        <Text color="brand.muted" fontSize="sm">
          Product list coming in Epic 12
        </Text>
      </VStack>
    </Layout>
  );
}
```

### Source Tree Location

```
src/pages/
├── dashboard.tsx              # Modified: Role-based router
├── producer/
│   └── dashboard.tsx          # New: Producer dashboard stub
├── distributor/
│   └── dashboard.tsx          # New: Distributor dashboard stub
└── retailer/
    └── dashboard.tsx          # New: Retailer dashboard stub
```

### Dependencies

- **Story 2.5 (DONE):** NextAuth.js authentication with role in session
- **Story 2.8 (DONE):** Original /dashboard stub (being replaced)
- **Story 4.1 (DONE):** Layout component, theme tokens

### Architecture Reference

From `docs/architecture/frontend-architecture.md`:
```
Authenticated Routes:
- /producer/dashboard - Product list
- /producer/register - Product registration form
- /distributor/dashboard - Received products
- /distributor/receive - QR scanner to receive products
- /retailer/dashboard - Stocked products
```

### Risks & Mitigations

| Risk | Mitigation |
|------|------------|
| Role not in session | Session includes role from Story 2.5, fallback to /login |
| Wrong role accesses page | getServerSideProps checks role, redirects to /dashboard |
| Bookmark to old /dashboard | /dashboard now redirects appropriately |

### Estimated Time

3 hours (S size)

## Testing

### Manual Testing

**Test User Setup (not pre-seeded):**
To test PRODUCER/DISTRIBUTOR/RETAILER dashboards:
1. Login as admin@foodtrace.app / admin123
2. Add Company (e.g., "Test Farm", domain: testfarm.local, type: PRODUCER)
3. Approve the company
4. Add Admin for that company (e.g., producer@testfarm.local / admin123)
5. Repeat for DISTRIBUTOR and RETAILER companies

**Test Steps:**
1. Login as each role, verify correct dashboard
2. Try accessing other role's dashboard directly → should redirect
3. Access /dashboard directly → should redirect to role-specific page
4. Test unauthenticated access to /producer/dashboard → redirects to /login

### No Automated Tests Required

Per workflow-decisions.md, UI stub pages don't require automated tests.

## Change Log

| Date       | Version | Description            | Author   |
| ---------- | ------- | ---------------------- | -------- |
| 2025-12-03 | 1.0     | Initial story creation | Bob (SM) |
| 2025-12-03 | 1.1     | Fixed Link/Button pattern to match codebase | Claude (Dev) |
| 2025-12-03 | 2.0     | Implementation complete | Claude (Dev) |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.5 (claude-opus-4-5-20251101)

### Debug Log References

- Session 59 (2025-12-03): Story 5.4 implementation
- Build successful with all 4 new dashboard pages
- All 124 tests pass (no regressions)

### Completion Notes List

1. Pre-implementation review identified Link/Button pattern issue in story template
2. Fixed story template to use `Button as={NextLink}` pattern (matches Layout.tsx)
3. Added test user setup instructions to manual testing section
4. Converted dashboard.tsx to role-based router with special handling for COMPANY_ADMIN
5. COMPANY_ADMIN kept on /dashboard with stub (no dedicated dashboard yet)
6. Created producer/dashboard.tsx with "Register Product" button
7. Created distributor/dashboard.tsx with Epic 6 placeholder
8. Created retailer/dashboard.tsx with Epic 7 placeholder
9. All pages use Layout component and theme tokens (brand.primary, brand.muted)
10. All pages have getServerSideProps role protection
11. Build passes, 124 tests pass

### File List

**Modified:**
- `src/pages/dashboard.tsx` - Converted to role-based router
- `docs/stories/5.4.story.md` - Fixed Link pattern, added test user setup

**Created:**
- `src/pages/producer/dashboard.tsx` - Producer dashboard stub (50 lines)
- `src/pages/distributor/dashboard.tsx` - Distributor dashboard stub (43 lines)
- `src/pages/retailer/dashboard.tsx` - Retailer dashboard stub (43 lines)

## QA Results

N/A - Routing/stub story, no QA review required per workflow-decisions.md
