# Story 9.4: Share Button and Responsive Polish

## Status

**TODO** - Ready for development

## Story

**As a** consumer,
**I want** to share a product's traceability page with friends,
**so that** I can show them where my food comes from and encourage them to check their own products.

## Acceptance Criteria

### Share Button
1. Share button displayed on product detail page header
2. Button has share icon (link/share icon from Chakra)
3. Clicking button copies current URL to clipboard
4. Toast notification confirms "Link copied to clipboard"
5. Button is accessible (proper aria-label)

### Responsive Polish
6. Page renders correctly at 360px width (iPhone SE)
7. Page renders correctly at 768px width (tablet)
8. Page renders correctly at 1280px width (desktop)
9. Product header stacks vertically on mobile (<768px)
10. Timeline events are readable on all screen sizes
11. Touch targets are at least 44px on mobile

### Consumer UX Polish
12. Page has proper meta tags for social sharing (og:title, og:description)
13. Loading states have proper feedback (spinner with text)
14. All text is readable without zooming (min 16px font)
15. Color contrast meets WCAG AA standards (already handled by theme)

## Tasks / Subtasks

- [ ] **Task 1: Implement Share Button** (AC: 1-5)
  - [ ] Add Share button to product header section
  - [ ] Import share/link icon from Chakra Icons
  - [ ] Implement clipboard copy using navigator.clipboard API
  - [ ] Add toast notification for success feedback
  - [ ] Add aria-label for accessibility

- [ ] **Task 2: Add Meta Tags for Social Sharing** (AC: 12)
  - [ ] Add next/head with og:title, og:description
  - [ ] Use product name and origin in meta tags
  - [ ] Add og:url with current page URL

- [ ] **Task 3: Responsive Polish** (AC: 6-11)
  - [ ] Test and adjust product header for mobile
  - [ ] Test timeline readability on mobile
  - [ ] Verify touch targets on all buttons
  - [ ] Use Chakra responsive props where needed

- [ ] **Task 4: Loading State Polish** (AC: 13-14)
  - [ ] Ensure loading spinner has descriptive text
  - [ ] Verify font sizes are at least 16px

## Dev Notes

### Story Type

**DEV STORY** - Frontend polish. No QA review required (UI improvements only).

### Epic Reference

**Epic 9: Consumer Query Interface**
- Assigned to: YiLing (Frontend Lead)
- Dependencies: Story 9.3 (timeline with registration)
- Blocks: None (final story in Epic 9)

### Technical Specifications

**Share Button Implementation:**

```typescript
// Add to src/pages/trace/[id].tsx
import Head from 'next/head';
import { LinkIcon } from '@chakra-ui/icons';

// In component:
const handleShare = async () => {
  try {
    await navigator.clipboard.writeText(window.location.href);
    toast({
      title: 'Link copied!',
      description: 'Share this link with friends to show them the product journey.',
      status: 'success',
      duration: 3000,
      isClosable: true,
    });
  } catch (err) {
    toast({
      title: 'Failed to copy',
      description: 'Please copy the URL from your browser address bar.',
      status: 'error',
      duration: 3000,
      isClosable: true,
    });
  }
};

// In JSX - Product Header:
<HStack justify="space-between" w="full">
  <Heading size="lg" color="brand.dark">
    {product.name}
  </Heading>
  <HStack>
    <Badge colorScheme="green" fontSize="sm">
      Product #{product.blockchainId}
    </Badge>
    <IconButton
      aria-label="Share product link"
      icon={<LinkIcon />}
      variant="ghost"
      onClick={handleShare}
      size="sm"
    />
  </HStack>
</HStack>
```

**Meta Tags for Social Sharing:**

```typescript
// In ProductTracePage component
<Head>
  <title>{product.name} - FoodTrace Journey</title>
  <meta
    name="description"
    content={`View the complete journey of ${product.name} from ${product.origin} to your table.`}
  />
  <meta property="og:title" content={`${product.name} - FoodTrace`} />
  <meta
    property="og:description"
    content={`See the complete supply chain journey of this product from ${product.origin}.`}
  />
  <meta property="og:url" content={`https://foodtrace.app/trace/${product.blockchainId}`} />
  <meta property="og:type" content="website" />
</Head>
```

**Responsive Header (Mobile Stack):**

```typescript
// Responsive product header
<Box w="full" bg="brand.surface" p={{ base: 4, md: 6 }} borderRadius="lg" borderWidth="1px">
  <VStack align="start" spacing={3}>
    {/* Title + Badge + Share - stack on mobile */}
    <Flex
      direction={{ base: 'column', md: 'row' }}
      justify="space-between"
      align={{ base: 'start', md: 'center' }}
      w="full"
      gap={2}
    >
      <Heading size={{ base: 'md', md: 'lg' }} color="brand.dark">
        {product.name}
      </Heading>
      <HStack>
        <Badge colorScheme="green" fontSize="sm">
          Product #{product.blockchainId}
        </Badge>
        <IconButton
          aria-label="Share product link"
          icon={<LinkIcon />}
          variant="ghost"
          onClick={handleShare}
          size="sm"
          minW="44px"
          minH="44px"
        />
      </HStack>
    </Flex>

    <Text color="brand.muted" fontSize={{ base: 'sm', md: 'md' }}>
      <strong>Origin:</strong> {product.origin}
    </Text>
    {/* ... other fields */}
  </VStack>
</Box>
```

### Source Tree Location

```
src/pages/trace/
└── [id].tsx               # MODIFY: Add share button, meta tags, responsive polish
```

### Dependencies

**Requires:**
- Story 9.3 - Timeline with registration event

**Blocks:**
- None (final story in Epic 9)

### Risks & Mitigations

| Risk | Mitigation |
|------|------------|
| Clipboard API not supported | Fallback toast with manual copy instruction |
| Meta tags not rendering (SSR issue) | Use next/head properly with server data |
| Touch targets too small | Use minW/minH props on buttons |

### Estimated Time

1 hour (XS size)
- Task 1: Share button - 0.25 hours
- Task 2: Meta tags - 0.25 hours
- Task 3: Responsive polish - 0.25 hours
- Task 4: Loading polish - 0.25 hours

## Testing

### Manual Test Checklist

**Share Button:**
- [ ] Share button visible in header
- [ ] Clicking copies URL to clipboard
- [ ] Toast appears confirming copy
- [ ] Button has proper aria-label

**Responsive Testing:**
- [ ] Page renders at 360px width (iPhone SE)
- [ ] Page renders at 768px width (tablet)
- [ ] Page renders at 1280px width (desktop)
- [ ] Header stacks on mobile
- [ ] All buttons have 44px touch targets
- [ ] Text is readable without zooming

**Social Sharing:**
- [ ] View page source shows og:title
- [ ] View page source shows og:description
- [ ] Meta tags include product name and origin

### Device Testing Matrix

| Device | Width | Test |
|--------|-------|------|
| iPhone SE | 375px | Manual |
| iPhone 12 | 390px | Manual |
| iPad | 768px | Manual |
| Desktop | 1280px | Manual |

## Change Log

| Date       | Version | Description                  | Author       |
| ---------- | ------- | ---------------------------- | ------------ |
| 2025-12-06 | 1.0     | Initial story creation       | Claude (SM)  |
