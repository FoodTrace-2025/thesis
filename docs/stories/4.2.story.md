# Story 4.2: Login Page UI

## Status

**Done**

## Story

**As a** user (Producer, Distributor, Retailer, or Platform Admin),
**I want** a clean, responsive login page,
**so that** I can authenticate and access my role-specific dashboard.

## Acceptance Criteria

1. Login page created at `/login` route
2. Email and password input fields with proper labels
3. "Login" button submits form (connected to NextAuth signIn)
4. "Remember me" checkbox (UI only for POC)
5. "Forgot password" link (UI only, non-functional for POC)
6. ~~Role information cards showing the 4 supply chain roles (illustrative)~~ **REMOVED** - See Scope Change below
7. Responsive layout (mobile-first, works on all screen sizes)
8. All colors use theme tokens (no hardcoded hex values)
9. Error states displayed for invalid credentials
10. Loading state shown during authentication

### Scope Change (2025-12-02)

**AC #6 removed:** Role information cards were removed from the login page because:
- Consumers don't need to login (they use wallet-free QR scanning at `/products/[id]`)
- Login page is only for business users who already know their role
- Cards added visual complexity without functional value
- Simplifies the login page to focus on authentication

## Tasks / Subtasks

- [x] **Task 1: Create Login Page Route** (AC: 1)
  - [x] Create `src/pages/login.tsx`
  - [x] Verify NextAuth redirects to `/login` correctly

- [x] **Task 2: Fix Naming and Branding** (AC: 1)
  - [x] Change page title to "FoodTrace Login"
  - [x] Change heading to "FoodTrace"

- [x] **Task 3: Centralize Colors in Theme** (AC: 8)
  - [x] Use theme tokens for all colors (brand.pageBg, brand.surface, brand.primary, etc.)
  - [x] No hardcoded hex colors in login.tsx

- [x] **Task 4: Connect Form to NextAuth** (AC: 3, 9, 10)
  - [x] Import `signIn` from `next-auth/react`
  - [x] Add form state management (email, password)
  - [x] Handle form submission with `signIn('credentials', {...})`
  - [x] Display error message on failed login
  - [x] Show loading state during authentication (Button isLoading)

- [x] **Task 5: Form Elements** (AC: 2, 4, 5)
  - [x] Email and password inputs with labels
  - [x] "Remember me" checkbox (UI only)
  - [x] "Forgot password" link (UI only)

- [x] **Task 6: Responsive Layout** (AC: 7)
  - [x] Centered card layout
  - [x] Responsive padding (base: 6, md: 8)
  - [x] Max width 400px for form card

## Dev Notes

### Story Type

**DEV STORY** - Frontend page implementation using Epic 4 components.

### Epic Reference

**Epic 4: Component Library - Week 3-4**
- Depends on: Story 4.1 (Theme, Layout, LoadingSpinner)
- Assigned to: YiLing (Lead), with fixes by Sam
- No QA required (UI page, easy to fix later)

### Technical Specifications

**NextAuth Integration:**

```typescript
// src/pages/login.tsx
import { signIn } from 'next-auth/react';
import { useState } from 'react';
import { useRouter } from 'next/router';

const handleSubmit = async (e: FormEvent<HTMLFormElement>) => {
  e.preventDefault();
  setIsLoading(true);
  setError("");

  const result = await signIn("credentials", {
    email,
    password,
    redirect: false,
  });

  setIsLoading(false);

  if (result?.error) {
    setError("Invalid email or password");
  } else {
    router.push("/dashboard");
  }
};
```

### Source Tree Location

```
src/
├── pages/
│   ├── login.tsx         # Login page (this story)
│   └── index.tsx         # Demo page (Story 4.1)
└── styles/
    └── theme.ts          # Theme with brand colors
```

### Dependencies

- **Story 4.1 (Required):** Theme, Layout, LoadingSpinner components
- **Story 2.5 (Required):** NextAuth.js configuration with CredentialsProvider
- **Story 2.8 Task 0 (Required for testing):** SessionProvider + /dashboard stub

### Risks & Mitigations

| Risk | Mitigation |
|------|------------|
| NextAuth redirect loop | Test signIn flow manually, check NEXTAUTH_URL |
| Form validation missing | Use Chakra's FormControl with isInvalid prop |
| Hardcoded colors remain | Verified: no hardcoded hex in login.tsx |

### Estimated Time

2-3 hours (S size) - Actual: ~3 hours including bug fixes and scope change

## Testing

### Manual Testing

1. Run `npm run dev` and open http://localhost:3000/login
2. Verify page renders with FoodTrace branding
3. Verify centered card layout with form
4. Enter invalid credentials → should show error message
5. Enter valid credentials → should redirect to /dashboard
6. Test on mobile viewport → should be responsive
7. Verify no hardcoded hex colors in source

### No Automated Tests Required

Per Epic 4 decisions, component tests deferred to Week 8 testing phase.

## Change Log

| Date       | Version | Description                              | Author   |
| ---------- | ------- | ---------------------------------------- | -------- |
| 2025-11-30 | 1.0     | Initial story creation from review findings | Bob (SM) |
| 2025-12-02 | 1.1     | YiLing initial implementation with role cards | YiLing |
| 2025-12-02 | 1.2     | Bug fixes: add type=submit, display error, remove unused code | Sam |
| 2025-12-02 | 2.0     | Scope change: remove role cards and Google button, simplify to clean login form | Sam |

## Dev Agent Record

### Agent Model Used

Claude Opus 4 (claude-opus-4-5-20251101)

### Debug Log References

N/A - No complex debugging required

### Completion Notes List

1. Initial implementation by YiLing had build errors and missing functionality
2. Sam fixed: button type=submit, error display, removed Chinese comments
3. Scope change decision: removed role cards (consumers don't login)
4. Final result: clean, simple 149-line login page

### File List

Files modified:
- src/pages/login.tsx (simplified from 264 to 149 lines)
- src/styles/theme.ts (colors already defined in Story 4.1)

## QA Results

N/A - UI page story, no QA review required per workflow-decisions.md
