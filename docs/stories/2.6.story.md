# Story 2.6: Create Company Admin User API

## Status

**Ready for Review**

## Story

**As a** platform administrator,
**I want** to create the first COMPANY_ADMIN user for an approved company,
**so that** the company owner can manage their own employees.

## Acceptance Criteria

1. POST /api/admin/users endpoint created and functional
2. Only PLATFORM_ADMIN role can access this endpoint (requirePlatformAdmin middleware)
3. Only for APPROVED companies (returns 400 for PENDING/REJECTED)
4. User email must match company domain (email.endsWith(`@${company.domain}`))
5. User role must be COMPANY_ADMIN (cannot create other roles via this endpoint)
6. Password hashed with bcrypt (10 rounds)
7. AuditLog entry created with action "CREATE_COMPANY_ADMIN"
8. Returns 404 for non-existent company ID
9. Returns 409 if email already exists
10. Response excludes passwordHash (security)

## Tasks / Subtasks

- [x] **Task 1: Create API Route** (AC: 1, 2)
  - [x] Create `src/pages/api/admin/users.ts`
  - [x] POST handler with requirePlatformAdmin middleware
  - [x] Return 405 for non-POST methods

- [x] **Task 2: Request Validation** (AC: 3, 4, 5)
  - [x] Create `src/lib/validation/user.ts` with createCompanyAdminSchema
  - [x] Zod schema: email (required), password (min 8 chars), name (required), companyId (required)
  - [x] Verify company exists and is APPROVED
  - [x] Validate email domain matches company domain
  - [x] Force role to COMPANY_ADMIN (ignore any role in request)

- [x] **Task 3: User Creation** (AC: 6, 7)
  - [x] Hash password with bcrypt (10 rounds)
  - [x] Create user with Prisma in transaction
  - [x] Create AuditLog entry in same transaction
  - [x] Handle duplicate email (unique constraint)

- [x] **Task 4: Response** (AC: 8, 9, 10)
  - [x] Return 201 Created with user data
  - [x] Exclude passwordHash from response
  - [x] Return 404 for company not found
  - [x] Return 409 for duplicate email

- [x] **Task 5: Write Tests** (AC: 1-10)
  - [x] Create `src/pages/api/admin/users.test.ts`
  - [x] Test: creates user with valid data (201)
  - [x] Test: returns 400 for missing required fields
  - [x] Test: returns 400 for invalid email format
  - [x] Test: returns 400 for email domain mismatch
  - [x] Test: returns 400 for company not approved (PENDING)
  - [x] Test: returns 400 for company not approved (REJECTED)
  - [x] Test: returns 404 for company not found
  - [x] Test: returns 409 for duplicate email
  - [x] Test: returns 401 for no session
  - [x] Test: returns 403 for non-PLATFORM_ADMIN
  - [x] Test: returns 405 for non-POST methods

## Dev Notes

### Story Type

**DEV STORY** - Standard implementation (no QA gate required)

> **QA Decision Note:** While user creation involves passwords (security-sensitive), this endpoint is restricted to PLATFORM_ADMIN only (not public registration), uses industry-standard bcrypt hashing, and follows established patterns from Story 2.2-2.5. Security patterns will be verified via code review during development.

### Dependencies

- **Story 2.5 (DONE):** requirePlatformAdmin middleware (`src/lib/auth/requireAdmin.ts`)
- **Story 2.1 (DONE):** User and Company models

### Business Logic Clarification

**Multiple COMPANY_ADMINs per company:** ALLOWED. A company can have multiple COMPANY_ADMIN users. No uniqueness check required beyond email uniqueness (which is enforced by Prisma schema).

### Request/Response Format

**POST /api/admin/users**

Request Body:
```json
{
  "email": "admin@producer.farm",
  "password": "SecurePass123!",
  "name": "Company Admin",
  "companyId": "test-producer-001"
}
```

Success Response (201 Created):
```json
{
  "success": true,
  "user": {
    "id": "clxyz123...",
    "email": "admin@producer.farm",
    "name": "Company Admin",
    "role": "COMPANY_ADMIN",
    "companyId": "test-producer-001",
    "createdAt": "2025-11-30T..."
  }
}
```

Error Response (400 Bad Request - Company not approved):
```json
{
  "error": "Company must be approved",
  "code": "COMPANY_NOT_APPROVED"
}
```

Error Response (400 Bad Request - Email domain mismatch):
```json
{
  "error": "Email must match company domain: producer.farm",
  "code": "INVALID_EMAIL_DOMAIN"
}
```

Error Response (404 Not Found):
```json
{
  "error": "Company not found",
  "code": "NOT_FOUND"
}
```

Error Response (409 Conflict - Duplicate email):
```json
{
  "error": "User with this email already exists",
  "code": "DUPLICATE_EMAIL"
}
```

### Validation Schema (src/lib/validation/user.ts)

```typescript
import { z } from 'zod';

/**
 * Schema for creating a COMPANY_ADMIN user
 * Used by POST /api/admin/users
 */
export const createCompanyAdminSchema = z.object({
  email: z.string().email('Invalid email format'),
  password: z.string().min(8, 'Password must be at least 8 characters'),
  name: z.string().min(1, 'Name is required').max(255, 'Name too long'),
  companyId: z.string().min(1, 'Company ID is required'),
});

export type CreateCompanyAdminInput = z.infer<typeof createCompanyAdminSchema>;
```

### File Location

```
src/
├── lib/validation/
│   ├── company.ts      # Existing company validation
│   └── user.ts         # NEW: User validation schemas
└── pages/api/admin/
    ├── companies/      # Existing company APIs
    ├── users.ts        # NEW: Create company admin user
    └── users.test.ts   # NEW: Tests
```

### Error Handling

| Error | Status | Response |
|-------|--------|----------|
| Validation failed | 400 | `{ error: "Validation failed", code: "VALIDATION_ERROR", details: [...] }` |
| Company not approved | 400 | `{ error: "Company must be approved", code: "COMPANY_NOT_APPROVED" }` |
| Email domain mismatch | 400 | `{ error: "Email must match company domain: {domain}", code: "INVALID_EMAIL_DOMAIN" }` |
| Company not found | 404 | `{ error: "Company not found", code: "NOT_FOUND" }` |
| Duplicate email | 409 | `{ error: "User with this email already exists", code: "DUPLICATE_EMAIL" }` |
| Not authenticated | 401 | `{ error: "Authentication required", code: "UNAUTHORIZED" }` |
| Not PLATFORM_ADMIN | 403 | `{ error: "Platform admin access required", code: "FORBIDDEN" }` |

### Risks & Mitigations

| Risk | Mitigation |
|------|------------|
| Duplicate email bypass | Prisma unique constraint + catch P2002 error |
| Password in logs | Never log password, only hash after creation |
| Email domain bypass | Server-side validation, never trust client |
| Unauthorized access | requirePlatformAdmin middleware enforces role |

### Estimated Time

2-3 hours (S size)

## Testing

### Test File Location

`src/pages/api/admin/users.test.ts`

### Test Framework

Jest + next-test-api-route-handler

### Test Commands

```bash
# Run all tests
npm test

# Run only user API tests
npm test -- users

# Run with coverage
npm test -- --coverage
```

### Coverage Target

>70% statement coverage for user creation endpoint

## Dev Agent Record

### Agent Model Used

claude-opus-4-5-20251101

### Completion Notes

- All 5 tasks completed successfully
- Implementation follows existing patterns from Story 2.2-2.5 company APIs
- All 12 tests pass (100% of specified test cases)
- Full test suite: 113 tests pass, no regressions
- Build successful with Next.js

### File List

| File | Action | Description |
|------|--------|-------------|
| `src/pages/api/admin/users.ts` | Created | POST /api/admin/users endpoint |
| `src/lib/validation/user.ts` | Created | Zod validation schema for user creation |
| `src/pages/api/admin/users.test.ts` | Created | 12 test cases covering all ACs |

## Change Log

| Date       | Version | Description            | Author   |
| ---------- | ------- | ---------------------- | -------- |
| 2025-11-30 | 1.0     | Initial story creation | Sam (SM) |
| 2025-12-01 | 1.1     | Pre-implementation review: Fixed middleware name to requirePlatformAdmin, added validation schema subtask, clarified multiple COMPANY_ADMINs allowed, updated file locations | Sam (Review) |
| 2025-12-01 | 1.2     | Added explicit test cases to Task 5 (11 tests), added QA decision note explaining why formal QA gate not required | Sam (SM) |
| 2025-12-01 | 1.3     | Added Dev Agent Record section (implementation complete, 12 tests pass) | James (Dev) |
